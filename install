#!/usr/bin/env bash
set -eo pipefail
[[ $DOKKU_TRACE ]] && set -x

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/config"
source "$PLUGIN_CORE_AVAILABLE_PATH/common/functions"
source "$PLUGIN_CORE_AVAILABLE_PATH/common/property-functions"

dokku_log_info1 "Installing dokku-uptime plugin"

mkdir -p "$PLUGIN_DATA_ROOT"
mkdir -p "$PLUGIN_CONFIG_ROOT"

fn-plugin-property-setup "uptime"

DOWNLOAD_URL="https://github.com/BigBoot/AutoKuma/releases/download/${KUMA_CLI_VERSION}/kuma-linux"

if [[ ! -f "$KUMA_BIN" ]] || [[ ! -s "$KUMA_BIN" ]] || ! "$KUMA_BIN" --version &>/dev/null; then
  dokku_log_info1 "Downloading kuma-cli ${KUMA_CLI_VERSION}..."
  curl -sSL "$DOWNLOAD_URL" -o "$KUMA_BIN"
  chmod +x "$KUMA_BIN"
  dokku_log_info1 "kuma-cli installed at $KUMA_BIN"
else
  dokku_log_info1 "kuma-cli already installed at $KUMA_BIN"
fi
